info:
  name: Get all jobs
  type: http
  seq: 1

http:
  method: GET
  url: "{{local_url}}/api/jobs/v1/getJobs?page=1&limit=10&search=005"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
    - name: search
      value: "005"
      type: query
  body:
    type: json
    data: "{\r\n    \r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
