info:
  name: Adresponse cron redis
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/adresponse/v1/adresponsePageCache?page=10&limit=50"
  params:
    - name: page
      value: "10"
      type: query
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
